# Sanity CMS (required for live CMS content; site falls back to local defaults without these)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production

# Only needed locally to run: npm run seed:sanity
SANITY_API_WRITE_TOKEN=your_editor_token
